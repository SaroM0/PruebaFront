// Se define la url base para las solicitudes a la API
const baseUrl = 
 'https://swapi.py4e.com/api/';

 // Se define la configuracion del entorno
export const environment = {
 production: false,
 // Se asigna la URL base a la propiedad baseUrl del objeto environment
 baseUrl
};
